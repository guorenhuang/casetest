# AI 平台 — 工程师笔试

- 形式：Take-home · 全流程 AI 协同
- 时长：建议 2 小时（48 小时内择时提交，实际耗时如实记录）
- 总分：100 分

---

## 关于这份笔试

我们团队 80% 以上的代码、运营脚本、自动化技能都是用 Claude Code / Codex 协同写出来的。所以我们考的不是"你能不能背八股、写算法"，而是：

- 你能不能用 AI 把一个真实业务问题从 0 推进到能交付
- 你能不能识别 AI 在哪里翻车，并把它拽回来
- 你的 AI 协同过程本身值不值得复制

技术栈不限，Python / Go / Java / TypeScript 任选你最顺手的。模型不限，Claude / GPT / Gemini 都行（**强烈推荐 Claude / Claude Code**，因为这是入职后日常主力工具）。资源不限，可以查文档、查 issue、参考开源（标注来源即可）。

**评分：过程 = 结果。** 一份 transcript 稀烂的满分代码，分数会低于一份 transcript 漂亮的半成品。我们要看的是你怎么想、怎么问、怎么识破 AI 的胡说八道、怎么把 AI 从沟里拉回正道。

---

## 提交要求（任一缺失按 0 分处理）

- [ ] **最终代码 / 产出物** —— 可运行的代码仓（zip 或私有 GitHub repo）+ 必要的 README
- [ ] **完整 AI 对话记录（transcript）** —— markdown 导出或截图序列，**覆盖全部 4 题**，不允许编辑美化（弯路就是弯路，原样保留）
- [ ] **一份 600 字以内的 Reflection**，回答以下三问：
  1. 哪一步 AI 最帮到你？（具体到某一次 prompt → output）
  2. 哪一步走了最多弯路？（AI 跑偏 or 你问错都算）
  3. 你是怎么把它拉回来的？如果重新做一遍，你会换哪种问法？

> 提交方式：打包 zip，文件树形如：
> ```
> exam-{你的名字}/
>   q1-shortdrama-backend/    # 代码 + transcript.md
>   q2-content-audit-agent/
>   q3-prompt-rescue/
>   q4-vibe-coding/
>   reflection.md
>   README.md                 # 总入口：每题怎么跑
> ```

---

## 题目 1 — 短剧后台：会员订阅 + 多渠道支付的小型可运行系统

**分值：30 分 · 建议时间：80 分钟**

### 场景

短剧 App 要上线"会员订阅"。后端需要一个最小可运行的服务，支持：

- 创建订阅订单（用户选套餐 → 后端生成订单）
- 接收并解析多渠道支付回调（至少模拟 **Stripe** 与 **Apple IAP** 两个渠道）
- 订单 / 订阅状态机：`pending → paid → active → expired / refunded`
- 幂等：同一笔支付回调被重复推送时不能重复发会员、不能重复扣减库存

### 硬性要求

1. **必须可运行**：一条命令（`docker compose up` 或同等）能起来；提供至少 1 条端到端 happy path 的 curl / httpie 脚本
2. **数据建模**：订单 / 订阅 / 支付事件 至少三张表，主键、外键、唯一索引、用于幂等的去重键写清楚
3. **支付回调用 mock**：你提供一个脚本，把伪造的 Stripe webhook / Apple IAP 通知 POST 到接口（请使用各自渠道真实的字段命名风格，不要随便起名）
4. **状态机必须显式**：用代码 / 注释 / 文档画出迁移图（`graph TD` 也可），非法迁移要有保护
5. **测试** 至少覆盖 3 种异常路径：重复 webhook 幂等、签名校验失败、未知 / 已过期订单 ID
6. **签名校验**：至少其中一个渠道（Stripe 推荐）写出真实风格的签名校验逻辑（HMAC + timestamp tolerance），不能裸接 webhook

### 交付物

```
q1-shortdrama-backend/
  src/                       # 服务代码
  schema.sql or migrations/  # 表结构
  state-machine.md           # 状态迁移图 + 文字说明
  scripts/                   # mock_stripe_webhook.sh / mock_apple_iap.sh / happy_path.sh
  tests/
  README.md                  # 启动 / 验证 / 设计取舍
  transcript.md              # 完整 AI 对话记录
```

---

## 题目 2 — 短剧评论审核 Agent / Skill：从一句话需求到可调度的工具

**分值：25 分 · 建议时间：60 分钟**

### 场景

运营丢给你一句话需求：

> "评论池太脏，里面有广告、引流、骂人、加微信号、用拼音 / emoji 绕开关键词的，一天上万条。请帮我搞一个自动审核工具：通过的让它通过，疑似的进人工，明确违规的直接屏蔽。"

你需要把这句话拆成一套可执行的方案，并实现一个**可独立运行**的 Skill / Agent（CLI 或 HTTP 都可），输入一批评论 → 输出 `pass / review / block` 三态 + 命中原因。

### 能力覆盖

至少包含以下识别能力（实现深浅自定，但每条必须能演示）：

- 敏感词词库
- 正则规则
- 拼音 / 谐音绕开识别（如"加 wei xin"、"jia v"、纯首字母 jvx）
- 连续数字（QQ 号 / 手机号 / 微信号格式）
- 图片 OCR（图床地址给你了；OCR 部分可以调真实 API，也可以 mock，但要写出接入点）

### 硬性要求

1. **规则不能写死在代码里**：规则配置必须可外部维护（yaml / json / DB 任选），新加一条规则不需要改业务代码、不需要重发版
2. **AI 判断必须有降级**：模型超时 / 不可用时，规则部分要能独立跑通并返回结果
3. **必须有命中证据**：每条评论的输出必须包含"为什么是这个判断"——命中哪条规则 / 模型给的 reason，能让运营回查
4. **造数据是考点**：你要构造至少 30 条评论（含正常、广告、骂人、绕开关键词、含图等），其中至少 5 条是**故意刁钻**的（比如 "加 V➕：18-XX-XXXX"、用 emoji 替代字符、纯拼音首字母、带配图广告等）。让 AI 帮你造，但你要 review 后说明取舍
5. **跑一次输出报告**：跑完 30 条之后产出一份 markdown 报告（每条结论 + 命中规则 / 模型 reason + 你认为是否合理）

### 交付物

```
q2-content-audit-agent/
  src/                     # Skill / Agent 代码
  rules.yaml or rules.json # 可外部维护的规则
  dataset.json             # ≥30 条评论（含 ≥5 条刁钻 case）
  run_report.md            # 跑一次的实际输出 + 你的复核
  README.md                # 架构 / 部署 / 如何添加新规则 / 如何替换模型
  transcript.md
```

---

## 题目 3 — 救火题：诊断一份"看起来没问题"的 Prompt

**分值：25 分 · 建议时间：60 分钟**

### 场景

某离职同事留下一份"短剧分镜生成 Agent"的 system prompt。投产 5 天后线上反馈：

- 有时输出特别长，把后续接口的 token / 解析逻辑撑爆
- 有时返回的根本不是合法 JSON，下游解析直接报错
- 有时同一个剧情输出风格漂忽（一会儿严肃悬疑、一会儿青春爱情）
- 偶尔会自己"改剧情"，把用户给的核心设定改没了

> **明确警告：这份 prompt 至少有 4 个隐藏问题。你的首要任务不是"凭感觉改"，而是先识别全部问题，给出证据，再修复。**

### 待诊断的 Prompt（请原样使用）

```
你是一个资深的短剧分镜师，请根据用户给的剧情，生成详细的分镜表。
请尽可能详细，越详细越好，把你能想到的都写出来。
输出 JSON 格式，字段你自己设计。
注意要符合年轻人喜欢的风格，有爆点。
如果剧情不合适，就自己改一下让它更适合短剧。
剧情：{plot}
```

### 硬性流程要求（违反扣分）

1. **不要立刻改 prompt**。先用 ≥5 条不同长度、不同题材（甜宠 / 悬疑 / 复仇 / 穿越 / 家庭伦理任选）的剧情真实跑一次，把原始输出贴出来
2. 列**问题清单**，每条问题都要给：现象 + 证据片段 + 为什么这是问题
3. 用 AI 协助诊断时要小心 —— **AI 经常会顺着你说"这个 prompt 写得挺好的"或"主要问题就是 XX"敷衍你**。你要用什么策略逼它真的发现问题？这一段博弈过程必须在 transcript 里看得到
4. 给出修复后的 prompt + 每一处改动对应解决了哪个问题（一一映射，不允许"一锅端")
5. 至少 2 组 before / after 对比，证明修复有效

### 交付物

```
q3-prompt-rescue/
  observations.md          # ≥5 次真实运行的原始输出
  issues.md                # 问题清单（每条带证据，至少 4 条）
  fixed_prompt.md          # 修复后 prompt + 改动 → 问题映射表
  before_after.md          # ≥2 组对比
  transcript.md            # 完整诊断过程对话
```

---

## 题目 4 — Vibe Coding 即兴：30 分钟造一个短剧团队的小工具

**分值：20 分 · 建议时间：40 分钟（编码 30 + 整理 10）**

### 场景

任选其一，从零在 30 分钟内交付一个**可运行**的小工具：

- **(a) 短剧标题批量改写器** —— CLI 或 Web，输入一批原标题（≥10 条），调用 AI 一次性改写为"更有点击欲"的版本，输出对比 CSV
- **(b) 分镜表 → 美术 prompt 转换器** —— 输入一份分镜表（你自己定 schema），逐条转换为可投喂给文生图 / 文生视频模型的英文 prompt，前端表格展示 + 一键导出
- **(c) 投放素材合规预检** —— 输入一段广告投放文案 + 一张配图（URL 即可），输出一份合规报告（命中风险 + 改写建议）

### 硬性要求（违反直接扣 50%）

1. **全程 Vibe Coding**：业务代码必须由 AI 生成 / 修改。改变量名、配 .env、复制依赖这种琐事可以手动；写业务逻辑必须靠 AI
2. **Transcript 带时间戳**：能看出哪次交互发生在第几分钟（IDE 时间戳 / 录屏截帧 / 手动标注都行）
3. **必须可运行**：允许有 bug，但核心 demo 路径要走得通
4. **必须标注一次"关键转折点"**：transcript 里框出那一次让项目从"卡住 / 一团乱"变"能跑"的 AI 产出，并用一句话说明它为什么是转折点
5. **必须标注一次"我不接受 AI 建议"的时刻**：AI 给了一个看似合理但你拒绝的建议，简述拒绝理由（如果整场都没拒绝过，写出来 —— 但通常这本身就是个信号）

### 交付物

```
q4-vibe-coding/
  code/                    # 实际代码
  README.md                # 选了哪个选题 + 为什么 + 如何运行
  transcript_with_ts.md    # 带时间戳的对话
  key_turning_point.md     # 关键转折点框选 + 说明
  rejected_suggestion.md   # 那次拒绝 + 理由
  demo.gif or demo.mp4     # 可选但推荐：≤10 秒 demo 录屏
```

---

## 评分维度

| 维度 | 分值 | 说明 |
|------|------|------|
| 技术正确性 | 20 | 代码跑不跑得通、数据建模合不合理、状态机 / 幂等 / 签名校验等基本功 |
| **AI 协同质量** | **30** | **权重最高** — transcript 反映出的"成熟工程师 × AI"的成熟度 |
| 问题反应力 | 20 | AI 跑偏时识别速度、纠偏效率、不被忽悠的能力（题 3 集中考察） |
| 工程化判断 | 15 | 是不是从"demo 能跑"推进到了"可交付"（日志 / 配置 / 文档 / 异常路径） |
| Reflection 深度 | 15 | 600 字复盘的诚实度与自我认知 |
| **合计** | **100** | |

---

## 常见问题

**Q1：技术栈可以混着用吗？**
A：可以。题 1 用 Go，题 2 用 Python，题 4 用 TS 都没问题。我们关心产出，不要求栈一致。

**Q2：Transcript 是不是要把每一句都贴？中间试错很丢脸。**
A：是，全保留。**我们就是要看试错。**没有弯路的 transcript 在我们眼里就是造假。你跑偏过、被 AI 带沟里过、然后爬出来——这才是我们想看的工程师。

**Q3：4 小时做不完怎么办？**
A：可以继续做。请在 reflection 里如实写实际耗时。我们不会因为超时直接扣分，但会和完成度综合评估。**不诚实的耗时会一票否决。**

**Q4：可以全程不写代码、只让 AI 写吗？**
A：题 4 就是这么要求的。题 1–3 可以你写、可以 AI 写，但 transcript 里必须能看出"你的判断"在哪里 —— 不是"AI 写啥我用啥"。

**Q5：模型 / 工具用哪个？**
A：不限。但 Claude / Claude Code 是我们日常主力，用它能直接体现入职后的工作方式，会有印象分。

**Q6：可以参考开源 / 自己以前写过的代码吗？**
A：可以参考，但**必须在 README / transcript 里注明来源**。直接大段复制粘贴你以前的项目代码而 transcript 里没有讨论过程，视同造假。

**Q7：我对短剧业务 / 支付 / OCR 不熟怎么办？**
A：让 AI 帮你快速建立领域认知，本身就是考察点。在 transcript 里展示这一段是加分项。

---

> 把 AI 当副驾驶，不是当代笔。
