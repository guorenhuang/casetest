# 评论审核批量报告（Q2 · 自动生成）

本报告由 `run_cli.py` **默认不调用 LLM**（需显传 `--use-llm`）生成，用于展示 **规则 + OCR 可独立跑通** 与每条 **命中证据**；线上可常驻开启 LLM 作为补充信号。

---

## c01

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
男主演技在线，这波反转可以。
```

## c02

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
女二造型好美，配乐也舒服。
```

## c03

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
第三集那句台词笑死我了😂纯娱乐别杠。
```

## c04

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
节奏有点慢但是还能追，坐等更新。
```

## c05

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
这剧三观挺正的，Families 爱看。
```

## c06

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
剧情有点牵强不过当个下饭剧还行。
```

## c07

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
有没有人记得第二集彩蛋细节？
```

## c08

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
结局有点仓促，但整体值回票价。
```

## c09

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
镜头语言不错，导演的构图有味道。
```

## c10

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
求别剧透😭我还在补前三集。
```

## c11

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
这演员以前演过另一部短剧也好看。
```

## c12

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
反派塑造比主角还立体，有点意思。
```

## c13

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
评论区氛围还行，别吵了大家和平讨论。
```

## c14

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
更新太慢了，制作方加油啊。
```

## c15

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
这集母题其实讲的是信任，细品还行。
```

## c16

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
有没有类似题材推荐？想一口气刷完。
```

## c17

- **结论**: `block`
- **证据**:
  - `rule` **PROFAN_LEXICON_L1**: [lexicon] 命中词表: 「傻逼」
- **是否合理（实现侧复核）**: 与标注预期 `block` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
你这发言像傻逼一样，滚远点。
```

## c18

- **结论**: `block`
- **证据**:
  - `rule` **PROFAN_LEXICON_L1**: [lexicon] 命中词表: 「尼玛」
- **是否合理（实现侧复核）**: 与标注预期 `block` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
你这智商真是尼玛离谱。
```

## c19

- **结论**: `block`
- **证据**:
  - `rule` **WECHAT_LEXICON_L2**: [lexicon] 命中词表: 「加微信」
  - `rule` **SOFT_PROMO_HOOK**: [regex] 正则命中: 「免费送」
- **是否合理（实现侧复核）**: 与标注预期 `block` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
加微信拉群，免费送全集资源百度云。
```

## c20

- **结论**: `block`
- **证据**:
  - `rule` **CONTACT_REGEX_ADV**: [regex] 正则命中: 「jia v」
  - `rule` **PINYIN_BYPASS_GAP**: [regex] 正则命中: 「jia v」
  - `rule` **SOFT_PROMO_HOOK**: [regex] 正则命中: 「主页」
- **是否合理（实现侧复核）**: 与标注预期 `block` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
jia v ❤️ 戳我主页看惊喜
```

## c21

- **结论**: `block`
- **证据**:
  - `rule` **CONTACT_REGEX_ADV**: [regex] 正则命中: 「wei xin」
  - `rule` **PINYIN_BYPASS_GAP**: [regex] 正则命中: 「wei xin」
- **是否合理（实现侧复核）**: 与标注预期 `review` **不一致**：可能因 URL/正则过宽或 LLM 关闭导致；建议收紧/放宽对应规则或补充词表。
- **合并文本截断**:

```text
wei xin 号发你了，加我领福利
```

## c22

- **结论**: `review`
- **证据**:
  - `rule` **URL_SHORTENER_REVIEW**: [regex] 正则命中: 「https://t.cn/FAKE123」
- **是否合理（实现侧复核）**: 与标注预期 `review` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
资源在这👇戳链接 https://t.cn/FAKE123
```

## c23

- **结论**: `review`
- **证据**:
  - `rule` **PHONE_QQ_PATTERN**: [digit_sequence] 连续数字 ≥7
- **是否合理（实现侧复核）**: 与标注预期 `review` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
我 QQ 号是 9574321801 私聊我拿
```

## c24

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
这集挺好看的推荐大家看看（无导流）。
```

## c25

- **结论**: `review`
- **证据**:
  - `rule` **SOFT_PROMO_HOOK**: [regex] 正则命中: 「免费送」
- **是否合理（实现侧复核）**: 与标注预期 `review` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
戳我免费送，磁力链接走起
```

## c26

- **结论**: `pass`
- **证据**:
  - _无命中项：规则/OCR 未触发；本条未启用 LLM，按 `pass` 放行（见设计要求：模型不可用仍可出结论）_
- **是否合理（实现侧复核）**: 与标注预期 `pass` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
纯讨论：伏笔是不是在茶杯那一场？
```

## c27

- **结论**: `block`
- **证据**:
  - `rule` **CONTACT_REGEX_ADV**: [regex] 正则命中: 「加V」
  - `rule` **EMOJI_SUBSTITUTION_TRAP**: [regex] 正则命中: 「V➕：18一23一55一99」
- **是否合理（实现侧复核）**: 与标注预期 `block` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
加V➕：18一23一55一99，懂的来
```

## c28

- **结论**: `block`
- **证据**:
  - `rule` **CONTACT_REGEX_ADV**: [regex] 正则命中: 「jia wei」
  - `rule` **PINYIN_BYPASS_GAP**: [regex] 正则命中: 「wei xin」
- **是否合理（实现侧复核）**: 与标注预期 `block` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
jia wei xin me 可以吗
```

## c29

- **结论**: `review`
- **证据**:
  - `rule` **PINYIN_BYPASS_GAP**: [regex] 正则命中: 「jvx」
- **是否合理（实现侧复核）**: 与标注预期 `review` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
jvx加我别犹豫
```

## c30

- **结论**: `block`
- **证据**:
  - `rule` **WECHAT_LEXICON_L2**: [lexicon] 命中词表: 「进群领」
  - `rule` **EMOJI_SUBSTITUTION_TRAP**: [regex] 正则命中: 「V📱💬戳我进群领红包888」
- **是否合理（实现侧复核）**: 与标注预期 `block` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
V📱💬戳我进群领红包888
```

## c31

- **结论**: `review`
- **证据**:
  - `rule` **SOFT_PROMO_HOOK**: [regex] 正则命中: 「私信」
- **是否合理（实现侧复核）**: 与标注预期 `review` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
私信我主页有惊喜（只是玩梗不构成真实导流语义）但仍易触发软广规则。
```

## c32

- **结论**: `review`
- **证据**:
  - `ocr` **ocr_adapter**: mock_ocr_param:ocr:https://cdn.quiz.test/img/ad.png?ocr=限量领取+进群+V123456789
  - `ocr` **ocr_text**: 识别文本片段: 限量领取 进群 V123456789
  - `rule` **EMOJI_SUBSTITUTION_TRAP**: [regex] 正则命中: 「V123456789」
  - `rule` **PHONE_QQ_PATTERN**: [digit_sequence] 连续数字 ≥7
- **是否合理（实现侧复核）**: 与标注预期 `review` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text

[OCR]
限量领取 进群 V123456789
```

## c33

- **结论**: `review`
- **证据**:
  - `ocr` **ocr_adapter**: mock_host_heuristic:https://img.example.com/frame.jpg
  - `ocr` **ocr_text**: 识别文本片段: [图内 OCR mock] V➕ 18812345678 限时进群
  - `rule` **EMOJI_SUBSTITUTION_TRAP**: [regex] 正则命中: 「V➕ 18812345678」
  - `rule` **PHONE_QQ_PATTERN**: [digit_sequence] 数字/分段号码模式命中
- **是否合理（实现侧复核）**: 与标注预期 `review` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
正常影评配图
[OCR]
[图内 OCR mock] V➕ 18812345678 限时进群
```

## c34

- **结论**: `review`
- **证据**:
  - `rule` **SOFT_PROMO_HOOK**: [regex] 正则命中: 「加我」
- **是否合理（实现侧复核）**: 与标注预期 `review` 一致，当前规则优先级可接受。
- **合并文本截断**:

```text
1️⃣8️⃣8️⃣-0000-neinei 加我
```

---

## 数据集与刁钻样本取舍

- 正常讨论、玩梗、剧情吐槽占多数，应大量 `pass`。
- 明显辱骂命中 `PROFAN_LEXICON_L1` → `block`。
- 显性「加微信」类命中词表或正则 → `block`。
- 拼音拆写、首字母、emoji+V+数字等 **刻意绕审** 样本推向 `review`，避免误杀正常英文/数字讨论。
- 图床 URL 走 `ocr_adapter` mock：query 参数或域名启发式注入「图中文字」，用于演示 **图片 OCR 证据**。
- 若启用 LLM：超时/异常时仍可返回上面规则结果（见 auditor 降级分支）。
